# Copy to .env (gitignored) and fill in. Only needed for npm run screenshot:ha.
# The script signs in through the standard HA username/password auth_provider.
HA_BASE_URL=http://localhost:8123
HA_USER=
HA_PASS=
# Optional: where to write the full-page screenshot.
# HA_OUTPUT=assets/dashboard.png
